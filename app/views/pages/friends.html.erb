  <div class="container mt-5 mr-2 text-center">
    <% @friends.each do |friend| %>
    <div class="card" style="width: 40rem;">
      <div class="card-body">
        <h5 class="card-title"><%= friend.first_name %></h5>
      </div>
    </div>
    <% end %>
    <h4>Pending requests</h4>
    <p><%= @user.pending_invitations %></p>
  </div>